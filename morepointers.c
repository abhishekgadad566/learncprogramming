#include<stdio.h>
int main()
{
    int *ptr=NULL;
    printf("the value inside variable ptr is:%d \n",ptr);

    void *p=NULL;
    printf("The size of the pointer is:5d\n",sizeof(p));

    int *pw;
    printf("\n%d",pw);

    return 0;
}